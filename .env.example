# Domain that tfvision will be served on.
# Add this to /etc/hosts pointing to 127.0.0.1 (see README).
TFVISION_DOMAIN=tfvision.test

# PostgreSQL credentials (used by backend and db services).
POSTGRES_USER=tfvision
POSTGRES_PASSWORD=tfvision
POSTGRES_DB=tfvision
